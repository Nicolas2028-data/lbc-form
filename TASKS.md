# LBC Care 改修タスクリスト (TASKS.md)

> SPEC.md の仕様に従い、Step 順に実装する。各 Step は完了条件をすべて満たしてから次へ進むこと。
> Step 1〜5 の開発・検証はステージング環境(SPEC 8)で行い、完了条件を満たしてから本番切替する。

---

## Step 0: 開発基盤

- [ ] 0-1. clasp を導入し、GAS プロジェクト(Code.gs)を既存 GitHub リポジトリに取り込む(`gas/` 配下)
- [ ] 0-2. `.clasp.json` / `appsscript.json` をコミット、push/pull の手順を README に追記
- [ ] 0-3. 現行の動作するコードでコミットを切る(ロールバック地点の確保)

**完了条件:** `clasp push` → GAS エディタに反映、`clasp pull` → ローカルに反映が双方向で確認できる。改修前スナップショットのタグ(`pre-refactor`)が存在する。

## Step 0.5: ステージング環境

- [ ] 0.5-1. 台帳シート(SPEC 2 の全タブ・列・保護設定)を staging 用に新規作成
- [ ] 0.5-2. Notion 顧客マスタDB・カルテDB を複製(空)し staging 用 ID を控える
- [ ] 0.5-3. スクリプトプロパティに `ENV` / `STAGING_*` を設定、`getConfig()` を実装
- [ ] 0.5-4. フロントの `?env=staging` 伝搬を実装

**完了条件:** `ENV=staging` で全既存機能が staging リソースのみを読み書きし、本番 Notion に一切書き込まれないことをログで確認。

## Step 1: 台帳+同期(コア)

- [ ] 1-1. 本番用台帳シート作成(SPEC 2。タブ・列・保護・onEdit トリガー・`_sync` セル)
- [ ] 1-2. `normalizePhone()` 実装+単体テスト関数(全角/ハイフン/+81/桁数異常のケース)
- [ ] 1-3. `nextCustomerId()` を LockService 化しシート台帳基準に変更
- [ ] 1-4. 受付系を「シート追記 → 即応答」に改修(施術台帳・クレジット台帳への書き込み、`_sync` インクリメント)
- [ ] 1-5. `syncToNotion()` 実装(SPEC 3: 早期リターン/ロック/updated_at>synced_at 抽出/upsert/synced_at 書き戻し/赤伝畳み込み/残高 SUM 書き込み/5回失敗スキップ+通知/sleep 350ms)
- [ ] 1-6. `installTriggers()` 実装(同期1分毎 + onEdit インストーラブル + 日次)と設置。毎時のフェイルセーフ全走査(SPEC 2.5)を含む
- [ ] 1-7. クレジットを台帳方式に切替(grant/use/expire、有効期限は付与時確定、FIFO 消し込み)

**完了条件:** staging で「問診票送信 → シートに行が入る → 1〜2分以内に Notion にページが生える」を確認。シート行を手編集(onEdit 発火)→ Notion に反映される。同期トリガー実行中に行編集しても次サイクルで拾われる(タイムスタンプ2列の動作確認)。`_sync=0` 時のトリガー実行時間が 1 秒未満。`_sync!A1` を故意に 0 へ書き換えた状態で未同期行を作り、フェイルセーフ全走査が回収することを確認。

## Step 1.5: データ移行

- [ ] 1.5-1. `migrateFromNotion()` 実装(SPEC 6)
- [ ] 1.5-2. staging で移行リハーサル(本番 Notion から読み、staging シートへ書く)
- [ ] 1.5-3. `verifyMigration()` で件数・残高一致を確認
- [ ] 1.5-4. 本番実行(本番シートへ)+検証

**完了条件:** verifyMigration が全チェック PASS。顧客件数・クレジット残高が Notion 表示値と一致。

## Step 2: 問診票の堅牢化+ルカスFB

- [ ] 2-1. 痛み部位の複数選択化(3言語、送信ペイロード・Notion 反映まで)
- [ ] 2-2. 「根本改善」設問の削除(フォーム・ペイロード・GAS・Notion 反映から完全除去)
- [ ] 2-3. 再来院フロー: 電話番号のみ入力 → 照合 → 本人確認表示。複数ヒット時の候補選択、0件時の初回誘導(3言語)
- [ ] 2-4. requestId(UUID)生成と GAS 側冪等性チェック(重複時は前回成功レスポンス)
- [ ] 2-5. 送信失敗時の localStorage 退避+再送ボタン+成功時クリア+送信中 disabled
- [ ] 2-6. 画像保存を Notion File Upload API 方式へ変更(新規分)
- [ ] 2-7. アクセスログタブへの全リクエスト記録
- [ ] 2-8. エラー時の NOTIFY_EMAIL 通知

**完了条件:** staging で ①同じ送信を3回再送しても台帳に1行のみ ②機内モードで送信失敗 → 復帰後に再送ボタンで成功、内容欠損なし ③再来院を電話番号だけで照合できる(1件/複数/0件の3パターン) ④人体図・署名が Notion ページに直接表示される ⑤意図的に例外を起こすと通知メールが届く。

## Step 3: 守り(認可)

- [ ] 3-1. スタッフ系 action 全てにサーバー側パスワード検証(SPEC 4.5)
- [ ] 3-2. CacheService による試行回数制限(10回/時)
- [ ] 3-3. 施術記録フロントの全呼び出しに password 同梱
- [ ] 3-4. Drive 過去画像の共有設定を「制限付き」に一括変更するユーティリティ実装+手動実行

**完了条件:** password なし/誤りで getPatientList・getPatientDetails・submitTreatmentRecord が全てエラー JSON を返す(curl で直接確認)。11回目の誤パスワードが試行制限エラーになる。Drive フォルダ内にリンク共有のファイルが残っていない。

## Step 3.5: 施術記録の訂正モード

- [ ] 3.5-1. 「取消して入力し直す」ボタン+確認ダイアログ
- [ ] 3.5-2. GAS 訂正処理(void 追記 → 新 record 追記 → 集計対象フラグ更新。SPEC 5.2)
- [ ] 3.5-3. 同期での赤伝畳み込み確認(Notion カルテが最新確定値の1ページになる)

**完了条件:** staging で ①訂正後の売上 SUM が正しい ②来院数(施術日×customer_id ユニーク)が水増しされない ③Notion カルテに取消行が並ばず最新値のみ表示 ④訂正リクエストの二重送信で void が2つ入らない。

## Step 4: 日次サマリメール

- [ ] 4-1. 時間トリガー(毎朝)で前日の施術件数・売上・クレジットの動き・同期エラー件数・アクセスログ異常(auth_fail 数)を NOTIFY_EMAIL へ送信

**完了条件:** staging データでサマリの数値が台帳の手計算と一致。

## Step 4.5: Looker Studio

- [ ] 4.5-1. 本番施術台帳をデータソースに接続
- [ ] 4.5-2. 3チャート作成(月別来院数/月別売上/新規・既存内訳。SPEC 9 の定義に厳密に従う)
- [ ] 4.5-3. ルカスの Google アカウントへ閲覧者共有(公開リンク禁止)
- [ ] 4.5-4. 埋め込み有効化 → Notion `/embed`。ルカスの実環境で表示確認、不安定なら直リンクにフォールバック

**完了条件:** 赤伝を含むテストデータで来院数・売上が正しい。レポート URL に第三者アカウントでアクセスして閲覧不可を確認。

## Step 5: i18n 分離

- [ ] 5-1. `i18n/{ja,es,pt}.json` へ文言分離(questionnaire.html / treatment-record.html のみ。index.html は触らない)
- [ ] 5-2. `js/common.js` へ共通処理集約(言語切替・fetch ラッパー・requestId・退避処理)
- [ ] 5-3. 3言語×2フォームの全画面を目視確認

**完了条件:** 文言変更が JSON 1箇所の編集で3画面に反映される。既存機能のリグレッションなし(Step 2 の完了条件を再実行して PASS)。

## Step 6: ドキュメント+本番切替

- [ ] 6-1. `docs/manual-lucas.md` 作成(SPEC 10 の5項目)
- [ ] 6-2. README 更新(アーキテクチャ図・運用ルール「シートが正/Notion は編集しない/過去日の訂正は赤伝手動追記」)
- [ ] 6-3. `ENV=production` へ切替、全フォームの本番疎通確認
- [ ] 6-4. 切替後1週間、日次サマリと同期エラーを監視

**完了条件:** 本番で問診票→Notion 反映、施術記録→売上反映、Looker Studio 更新、の一連が動作。マニュアルをルカスが読んで訂正操作を自力で実行できる。

---

## 実装上の注意(全 Step 共通)

- index.html と予約系 API(getSlots/submitBooking/JSONP)には一切触れない
- ID・メールアドレス・パスワードのハードコード禁止(getConfig 経由)
- 台帳の既存行を書き換えるコードは「集計対象フラグ更新(Step 3.5)」以外に書かない
- 各 Step 完了ごとにコミット。メッセージに Step 番号を含める

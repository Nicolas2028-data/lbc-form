# LBC Care システム改修 実装仕様書 (SPEC.md)

> この文書は Claude Code への実装指示書である。既存リポジトリ(lbc-form)に対する改修を行う。
> 作業順序と完了条件は TASKS.md を参照。本書は「何を・なぜ・どう作るか」の正である。

---

## 0. プロジェクト概要

- **対象:** 三重県四日市市の整体院 LBC Care のオペレーション管理システム
- **規模:** 月6〜12人。術者1人(ルカス)+開発者1人(ニコラス)
- **現行構成:** GitHub Pages(静的HTML 3枚) → GAS Webアプリ(Code.gs) → Notion API / Drive / Gmail
- **今回のスコープ:** 問診票(questionnaire.html)+施術記録(treatment-record.html)+Notion の運用基盤を完成させる
- **スコープ外(凍結):** 予約フォーム(index.html)は**一切触らない**。予約関連機能(getSlots・submitBooking・リマインダー・予約枠の排他制御)は改修対象外
- **コスト制約:** すべて無料枠(GAS/Sheets/Notion/Looker Studio 無料プラン)で実現すること

---

## 1. 目標アーキテクチャ(直列パイプライン)

```
[iPad / フォーム (GitHub Pages)]
        │ fetch POST/GET
        ▼
[GAS 受付 (doPost/doGet)]
  - 検証・冪等性チェック・採番
  - シート台帳に追記して即応答
  - 即時性が必要な処理(通知メール)のみここで実行
        ▼
[Google スプレッドシート = 原本 (Source of Truth)]
  - 追記専用の台帳タブ群 + 編集可のマスタタブ
        ▼
[GAS 同期トリガー (時間駆動・1分毎)]
  - 未同期行を拾って Notion API へ upsert
        ▼
[Notion = 表示専用ダッシュボード]
  - ルカスが日常的に見る画面。データの原本ではない
```

### 設計原則(全実装がこれに従うこと)

1. **シートが正。** シートと Notion が食い違ったら常にシートを信じる。Notion 側の直接編集は運用ルールとして禁止(システムは Notion からの読み戻しを一切行わない)
2. **台帳は追記専用。** 「起きたこと」(施術・売上・クレジットの動き)は行の書き換え禁止。訂正は赤伝方式(取消行+訂正行の追記)
3. **マスタは編集可。** 「今の状態」(顧客の連絡先等)は上書き編集してよい。編集は再同期対象になる
4. **タイムスタンプ2列同期。** 同期は真偽値フラグではなく `updated_at` / `synced_at` の比較で行う(送信中編集の取りこぼし防止)
5. **冪等性。** すべての書き込み系リクエストはクライアント生成の UUID を持ち、同一 UUID の再送は無視される(結果は同じレスポンスを返す)
6. **論理削除のみ。** 台帳・マスタとも物理削除禁止。取消はステータス/種別で表現する
7. **認可はサーバー側。** スタッフ系 API は毎リクエスト、サーバー側でパスワード検証する。フロントの画面ゲートは UX であって認可ではない

---

## 2. スプレッドシート設計

新規スプレッドシートを1つ作成する(名称: `LBC台帳`)。ID はスクリプトプロパティ `LEDGER_SPREADSHEET_ID` で管理。

### 2.1 タブ: `顧客マスタ` (編集可)

| 列 | 名前 | 型/形式 | 備考 |
|----|------|---------|------|
| A | customer_id | P001形式 | 既存の診察番号体系を継続 |
| B | 名前 | text | |
| C | フリガナ | text | 正規化して保存(全角カナ統一) |
| D | 電話番号 | text | **正規化済み**(下記 4.2)。照合の主キー |
| E | email | text | |
| F | 生年月日 | yyyy-MM-dd | |
| G | 初回訪問日 | yyyy-MM-dd | |
| H | 言語 | ja/es/pt | |
| I | 来院のきっかけ | text | |
| J | 住所 | text | |
| K | ステータス | active/archived | 論理削除用 |
| L | notion_page_id | text | Notion 顧客マスタDBの対応ページID |
| M | created_at | ISO8601 | |
| N | updated_at | ISO8601 | onEdit / スクリプトが更新 |
| O | synced_at | ISO8601 | 同期トリガーが更新 |

### 2.2 タブ: `施術台帳` (追記専用)

| 列 | 名前 | 型/形式 | 備考 |
|----|------|---------|------|
| A | entry_id | UUID | 冪等性キー兼行ID |
| B | 種別 | record/void/correction | record=通常, void=取消(赤伝), correction=訂正 |
| C | target_entry_id | UUID | void/correction が指す元行。record は空 |
| D | 施術日 | yyyy-MM-dd | |
| E | customer_id | P001形式 | |
| F | コース | text | 既存 VALID_COURSES の値のみ |
| G | 売上金額 | number | void は負数で記録 |
| H | 支払い方法 | 現金/カード/PayPay/未払い | |
| I | 施術メモ | text | |
| J | 問診票提出 | TRUE/FALSE | |
| K | クレジット使用額 | number | |
| L | 紹介者customer_id | text | |
| M | 集計対象 | TRUE/FALSE | **来院数カウント用。** record=TRUE、void された record と void/correction 行は FALSE に更新(唯一許される既存行更新。詳細 5.2) |
| N | notion_page_id | text | 対応するカルテページID |
| O | created_at / P updated_at / Q synced_at | ISO8601 | |

### 2.3 タブ: `クレジット台帳` (追記専用)

| 列 | 名前 | 備考 |
|----|------|------|
| A | entry_id | UUID |
| B | 日付 | yyyy-MM-dd |
| C | customer_id | |
| D | 種別 | grant(付与)/use(消費)/expire(失効)/void(取消) |
| E | 金額 | grant は正、use/expire は負、void は打ち消し符号 |
| F | 有効期限 | grant 時に **付与日+1年を確定値として保存**(動的計算をやめる) |
| G | 関連entry_id | 施術台帳の entry_id または打ち消し対象 |
| H | created_at / I synced_at | |

残高 = 該当 customer_id の金額列 SUM。FIFO 消費は grant 行を有効期限昇順に消し込む(消し込み状態は都度 SUM+期限で導出し、状態列は持たない)。

**業務ルールの実装(現行コードに欠落):** ①紹介による grant は同一紹介者につき**最大3件**まで(grant 前に該当顧客の紹介 grant 行数を数え、上限到達時は付与せず理由をレスポンスとログに残す)。②use は残高超過をエラーにする(現行の `useCredit` は超過分を黙って切り捨てており、これを踏襲しない)。

### 2.4 タブ: `アクセスログ` (追記専用)

全 doPost/doGet 呼び出しを1行ずつ記録: timestamp, action, 冪等性キー, 結果(ok/error/duplicate/auth_fail), エラー概要, 処理時間ms。個人情報の生値(名前・電話全桁)は書かない(customer_id と電話下4桁まで)。

### 2.5 タブ: `_sync` (システム用・1セル運用)

`A1` = 未同期件数カウンタ(受付が書き込み時にインクリメント、同期完了時に再計算)。同期トリガーは冒頭でこのセルだけ読み、0 なら即終了する(早期リターン。1分毎トリガーをクォータ内に収める要)。

**フェイルセーフ:** カウンタは同時実行でずれうるヒントに過ぎない。毎時 0 分の実行回(または `B1` に前回全走査時刻を持ち 60 分経過時)は、カウンタが 0 でも全タブの `updated_at > synced_at` 走査を必ず実行し、取りこぼしを回収してカウンタを再計算する。

### 2.6 シート保護

- `施術台帳` `クレジット台帳` `アクセスログ` `_sync`: タブ保護を設定。**注意:** シート保護はオーナー自身の編集を阻止できない(Google の仕様)。これは誤編集への警告ガードレールであり、ハードな防御ではないことを前提とする
- `顧客マスタ`: ニコラスの手動編集を許可。編集行の `updated_at` を現在時刻に更新する onEdit を実装(複数行ペースト対応: e.range の全行を処理)
- **onEdit はインストーラブルトリガーで作ること。** 本 GAS はシートに紐付かないスタンドアロンの Web アプリのため、単純トリガー(関数名 onEdit)は発火しない。`ScriptApp.newTrigger('onLedgerEdit').forSpreadsheet(LEDGER_SPREADSHEET_ID).onEdit().create()` で明示的に設置する(セットアップ関数 `installTriggers()` に集約し、同期トリガー・日次トリガーもここで一括設置)

---

## 3. 同期トリガー仕様 (`syncToNotion`)

- **駆動:** 時間主導型トリガー、1分毎
- **早期リターン:** `_sync!A1` が 0 なら即 return
- **ロック:** LockService(スクリプトロック)で多重起動を防止。取れなければ即 return
- **対象抽出:** 各タブで `updated_at > synced_at`(synced_at 空含む)の行
- **upsert:** `notion_page_id` が空 → Notion にページ新規作成しIDを書き戻し / あり → そのページを更新
- **synced_at の書き方:** 「送信前に読み取った updated_at の値」を書く(送信中に再編集された行が次サイクルで必ず拾われるように)
- **赤伝の畳み込み:** Notion カルテには void/correction 行をそのまま出さない。record 行のページを「最新確定値」で更新する(void された record のカルテはステータスを取消に変更)。生の赤伝履歴はシートで見る
- **クレジット残高:** 顧客の Notion ページの残高プロパティは、同期時にクレジット台帳の SUM を書き込む(Notion 側で計算しない)
- **エラー処理:** 行単位で try/catch。失敗行は synced_at を書かず次回リトライ。失敗回数は各タブに `sync_error_count` 列を追加して行内で管理し、5 回連続失敗したらエラー通知メールを送って以降スキップ(count>=5 を抽出条件から除外。無限リトライでトリガーを詰まらせない)。手動対応後は count を 0 に戻せば再同期される
- **レート制限:** Notion API 呼び出し間に Utilities.sleep(350) を挟む(3req/s 遵守)

---

## 4. GAS 受付 (doPost/doGet) 改修仕様

### 4.1 冪等性

- 全 POST リクエストは `requestId`(フロント生成 UUID)を必須にする
- 受付は最初に該当台帳/ログで requestId の既出をチェック。既出なら **前回と同じ成功レスポンス**を返す(duplicate としてログ)

### 4.2 電話番号正規化 (`normalizePhone`)

- 対象は日本の番号のみ(海外番号の考慮不要)
- 処理: 全角→半角、ハイフン・空白・括弧除去、`+81` 始まりは `0` 始まりに変換。結果は `0` 始まり 10〜11 桁。桁数不正はバリデーションエラー
- **照合・保存のすべてでこの関数を通す。** 生の入力値で比較するコードを残さないこと

### 4.3 顧客照合(問診票・再来院フロー) ★ルカスFB反映

- **再来院:** 問診票の再来院モードは**電話番号のみ**の入力で照合する
  - 正規化電話番号でマスタ検索 → 1件ヒット: その顧客に確定し、氏名を画面表示して本人確認(「〇〇様ですね?」)
  - 複数ヒット(家族で同番号等): 候補の氏名リストを返し、フロントで選択させる
  - 0件: 「見つかりません」を返し、初回モード(全項目入力)へ誘導
- **初回:** 全項目入力 → 正規化電話番号で重複チェック → 重複時は既存顧客として扱い情報を更新(新規行を作らない)
- 名前の完全一致照合は廃止する
- **送信時のサーバー側再解決:** 現行コード(`handleSubmitAll` の再来院パス)はフロントが渡す `customerId` を無検証で信用しているが、改修後は再来院の問診送信にも電話番号を同梱させ、**サーバー側で電話番号から customer_id を再解決**する(照合時と送信時で同じ関数を通す)。フロント由来の customerId は補助情報(複数ヒット時の本人選択結果)としてのみ使い、電話番号との整合をサーバーで検証する

### 4.4 採番 (`nextCustomerId`)

- LockService(スクリプトロック)を取得してから、顧客マスタタブの customer_id を**数値としてパースして最大値**+1 を発行し、行追記までロック内で行う(現行の `generatePatientNumber` は Notion の rich_text 文字列降順ソートに依存しており、P999→P1000 で破綻する。流用しない)
- P001 形式を継続。桁あふれ(P999 超)は P1000 と自然に伸ばす

### 4.5 スタッフ認可

- 方式は**パスワードのみ**(トークン化はしない。ニコラス判断済み)
- **スタッフ系アクションは全て POST に統一する。** 現行の `verifyStaff` / `getPatientList` / `getPatientDetails` は GET でパスワードやIDが URL クエリに載る設計のため、POST ボディに移す(URL・アクセスログへの残留防止)。フロントの呼び出しも合わせて変更
- 検証は**サーバー側・毎リクエスト**: スタッフ系 action(verifyStaff / getPatientList / getPatientDetails / submitTreatmentRecord / 訂正系)は全て `password` パラメータを要求し、スクリプトプロパティ `STAFF_PASSWORD` と照合。不一致は 401 相当のエラー JSON
- **試行回数制限:** CacheService で直近失敗回数を記録し、10回/時を超えたら一定時間拒否
- 患者向け action(問診票 submit 系)はパスワード不要のまま

### 4.6 エラー通知

- doPost/doGet の最外周 try/catch で、例外時に `MailApp.sendEmail` で通知(宛先はスクリプトプロパティ `NOTIFY_EMAIL`)。通知にはアクション名・エラー概要・ログ行参照を含める(個人情報の生値は含めない)

### 4.7 画像保存

- **新規分:** Drive 保存をやめ、**Notion File Upload API** でカルテページに直接アップロードする方式へ回帰(embed 不安定問題と公開リンク問題を同時に解消)
  - GAS からの実装: ①`POST /v1/file_uploads` で upload オブジェクト作成 → ②UrlFetchApp で `payload: { file: blob }` を渡して multipart/form-data 送信(base64 → `Utilities.newBlob` で PNG blob 化)→ ③カルテページに file ブロックとして添付。Canvas 由来の PNG は数百 KB 程度でシングルパート上限(20MB)に収まる
  - 実装前にニコラスが手動確認: 使用中の Notion インテグレーションで File Upload API が許可されているか(SPEC 末尾の手作業リスト参照)
- **過去分:** Drive に残置。ただし対象フォルダ配下の既存ファイルの共有設定を「制限付き」に一括変更するユーティリティ関数を1本用意し、手動実行する(過去カルテ上の embed 表示が壊れることは許容済み)

---

## 5. フロントエンド改修仕様

### 5.1 問診票 (questionnaire.html) ★ルカスFB反映

1. **痛み部位の複数選択化:** 現在単一選択になっている「どこが痛いか」を複数選択(チェックボックス or 人体図の複数マーク)に変更。データは配列で送信し、Notion には multi_select 相当で反映
2. **「根本改善」選択肢の削除:** 施術目的(`treatmentGoal`)の選択肢 `root_cause`(「根本改善を目指したい」)を、フォームの選択肢(3言語)・GAS の `VALUE_LABEL` から削除する。設問自体(施術目的)は残す
3. **再来院フロー:** 4.3 の通り、電話番号のみ入力 → 照合 → 本人確認表示。3言語(ja/es/pt)すべてで対応
4. **送信の堅牢化:**
   - 送信ごとに requestId(UUID)を生成して同梱。**`crypto.randomUUID()` は iOS 15.4 未満の Safari に存在しないため、共通関数 `genRequestId()` を用意し、未対応環境では `crypto.getRandomValues` ベースの UUID v4 フォールバックを使う**(院の iPad の OS バージョンに依存させない)
   - 送信失敗時: 入力内容(画像の dataURL 含む)を localStorage に退避し、「再送信」ボタンを表示。再送は同じ requestId を使う(冪等)
   - **退避のサイズガード:** 退避前に合計サイズを概算し、localStorage 上限(約5MB)に収まらない場合は画像を JPEG 品質 0.7 で再エンコードして縮小、それでも超える場合はテキスト項目のみ退避して「画像は再入力が必要」と表示する(退避処理自体の例外で送信データを失わないこと)
   - 送信成功時: 退避データを削除
   - 二重タップ防止: 送信中はボタンを disabled

### 5.2 施術記録 (treatment-record.html)

1. **訂正モード(最小版):** 当日分の自分の記録に対して「取消して入力し直す」ボタンを1つ追加
   - 押下 → 確認ダイアログ → GAS へ訂正リクエスト(元 entry_id を指定)
   - GAS 側処理: ①void 行を追記(売上は負数、target_entry_id=元行)、②新しい record 行を追記、③元 record 行と void 行の `集計対象` を FALSE に更新(注: これは赤伝集計を正しく保つための、追記専用原則の明示的な例外。ここ以外で既存行を書き換えるコードを書かないこと)
   - 過去日の訂正は UI 化しない(ニコラスがシートに赤伝行を手動追記する運用)
2. **認可:** 全 API 呼び出しに password を同梱(4.5 準拠)
3. **冪等性:** 5.1-4 と同じ requestId 方式

### 5.3 i18n 分離(最終フェーズ)

- 3ファイルに埋め込まれた文言を `i18n/ja.json` `i18n/es.json` `i18n/pt.json` に分離
- 共通処理(言語切替・翻訳適用・requestId 生成・fetch ラッパー・失敗時退避)を `js/common.js` に括り出す
- **注意:** index.html(予約)は凍結中のため、i18n 分離の対象は questionnaire.html と treatment-record.html のみ。index.html は触らない

---

## 6. データ移行(Notion → シート)

一度だけ実行する移行スクリプト `migrateFromNotion()` を書く。**Notion クエリは必ずページネーション(has_more / next_cursor)を実装すること**(現行コードは page_size:100 固定で 100 件超を取得できない。これを流用しない):

1. Notion 顧客マスタDB を全件取得 → `顧客マスタ` タブへ(notion_page_id を保存、電話番号は正規化して保存、updated_at=synced_at=移行時刻)
2. Notion 施術カルテDB を全件取得 → `施術台帳` タブへ(種別=record、集計対象=TRUE、notion_page_id 保存)
3. 顧客マスタの `クレジット詳細` JSON を解析 → `クレジット台帳` の grant 行に展開。**注意: 現行実装ではこの JSON は「消費後の残存 grant のみ」を保持している**(useCredit が消費時に配列から減額する)ため、各エントリをそのまま grant 行にすればよい(有効期限=date+1年を確定保存)。展開後、SUM が Notion の `クレジット残高` と一致するか検証し、不一致分は調整行(種別 grant/void、メモ付き)で埋めて一致させる
4. 移行結果サマリ(件数・スキップ・警告)をログ出力
5. **移行後の検証関数** `verifyMigration()`: 顧客件数一致・クレジット残高一致(シートSUM vs Notion 表示値)をチェックしレポート

---

## 7. スクリプトプロパティ(新規・変更)

| キー | 説明 |
|------|------|
| `LEDGER_SPREADSHEET_ID` | 台帳シートID(本番) |
| `NOTIFY_EMAIL` | `kakimorilucas@gmail.com` |
| `ENV` | `production` / `staging` |
| `STAGING_*` | ステージング用の各ID(下記 8) |
| 既存: `NOTION_TOKEN` `CUSTOMER_DB_ID` `KARTE_DB_ID` `STAFF_PASSWORD` | 継続使用 |

コード内では `getConfig()` 1関数に集約し、`ENV` に応じて本番/ステージングの ID セットを返す。ID を直書きしない。

---

## 8. テスト環境(ステージング)

- 台帳シートの複製 + Notion の顧客マスタDB/カルテDB の複製(空でよい)を作成し、`STAGING_LEDGER_SPREADSHEET_ID` 等に設定
- GAS は同一プロジェクトで `ENV=staging` に切り替えて開発・検証 → 全タスクの完了条件を満たしたら `ENV=production` へ
- フロントは `?env=staging` クエリで GAS に env を伝える(GAS 側で env パラメータ受理は staging プロパティ存在時のみ有効)
- **本番の患者データで実験しないこと**

---

## 9. Looker Studio(台帳完成後)

1. データソース: `施術台帳` タブ(Google スプレッドシート公式コネクタ・無料)
2. 必須チャート: ①月別来院数(棒。集計対象=TRUE の行の customer_id ユニークカウント…来院数、または entry カウント…施術数。**来院数=施術日×customer_id のユニーク数** とする)、②月別売上(棒。売上金額の SUM — 赤伝込みで自動的に正しくなる)、③新規/既存内訳(①の棒を、顧客の初回訪問日がその月か否かで色分け)
3. 共有: レポートをルカスの Google アカウントに閲覧者として共有(公開リンクにしない)
4. Notion 埋め込み: File → レポートを埋め込む → 埋め込み有効化 → URL を Notion `/embed`。非公開のまま埋め込み可能(閲覧者は Google ログイン必須)。ルカスの閲覧環境で表示確認し、アプリで不安定なら直リンク方式にフォールバック

---

## 10. ルカス向けマニュアル(成果物)

`docs/manual-lucas.md` として作成(将来 Notion に転記する前提の日本語・平易な文章+表):

1. 施術記録の入力手順(通常)
2. **間違えたときの直し方**(訂正モードの使い方: いつ使える/使えない、押した後何が起きるか)
3. 問診票 iPad 運用手順(再来院は電話番号だけ、の説明含む)
4. ダッシュボード(Notion/Looker Studio)の見方と「Notion は編集しない」ルール
5. 困ったとき(送信失敗時の再送ボタン、ニコラスへの連絡基準)

---

## 11. ニコラスの手作業リスト(Claude Code には実行不可能な作業)

1. Notion インテグレーションで **File Upload API が使えることの確認**(Step 2 着手前)
2. **ステージング用 Notion DB(複製)をインテグレーションに接続**(Step 0.5 着手前)
3. スクリプトプロパティへの実値設定(`NOTIFY_EMAIL` 等の秘匿値)
4. Looker Studio のレポート作成・共有操作(Step 4.5。仕様は SPEC 9 に従い、Claude Code は手順書の生成まで)
5. Drive 過去画像の共有変更ユーティリティの**実行**(コードは Claude Code が書く。実行と結果確認はニコラス)

## 12. やらないこと(明示)

- 予約フォーム(index.html)・予約系 API の変更(凍結。閉鎖もしない)
- スタッフ認証のトークン化・セッション管理(パスワード方式を維持)
- 過去 Drive 画像の Notion への再アップロード
- Notion からシートへの逆同期(シートが正、の一方向のみ)
- 有料サービスの導入
